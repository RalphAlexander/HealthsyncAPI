To load Database:
1. Copy the folder "healthsync" into your temp directory (For example C:\temp on windows)
2. Start up postgres
3. Run build script in postgres by entering "\i /temp/healthsync/build.sh" without the quotations.

To edit data: 
- Modify table creation in build_db.txt
- Modify table data in respective .txt file 